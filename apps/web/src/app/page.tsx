/** Step 01 placeholder: the upload screen's heading only; the dropzone and validation panel are not built yet. */
export default function Page() {
  return (
    <div className="p-6 md:p-8">
      <h1 className="text-3xl font-bold">Upload your portfolio</h1>
      <p className="mt-2 text-muted">A CSV or XLSX of your own stores.</p>
    </div>
  );
}
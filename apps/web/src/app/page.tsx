/** Step 01 placeholder: the upload screen's heading only, until Phase 2 builds the dropzone and validation panel. */
export default function Page() {
  return (
    <div className="p-6 md:p-8">
      <h1 className="text-3xl font-bold">Upload your portfolio</h1>
      <p className="mt-2 text-muted">A CSV or XLSX of your own stores. Arrives in Phase 2.</p>
    </div>
  );
}